export default function Loading({ padding = '80px 0' }) {
  return (
    <div className="loading-grid" style={{ display: 'flex', padding }}>
      <span />
      <span />
      <span />
    </div>
  );
}
